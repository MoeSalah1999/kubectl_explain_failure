name: PVCPendingTooLong
category: PersistentVolumeClaim
priority: 15
requires:
  objects:
    - pvc
if: >
  context.get('timeline') is not None
  and timeline_has_pattern(
        context.get('timeline', []),
        [{'kind': 'PersistentVolumeClaim', 'phase': 'Pending'}]
      )
then:
  root_cause: "PVC has been Pending too long"
  confidence: 0.9
  evidence:
    - "PVC is Pending"
  suggested_checks:
    - "Check StorageClass provisioner logs"
